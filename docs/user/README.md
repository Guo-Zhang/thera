# 用户指南

入口：命令行
交互方式：对话
内部架构：AI 根据需要自动切换到内置模式，比如思考模式。也可以由用户写命令切换。

可选模式：思考模式、写作模式、知识工程模式等等。

默认存储路径：用户根目录下的/thera 文件夹。
存储提交与备份：根据使用习惯再做决定。优先 GitHub，次选阿里云。

备份与安全：禁止直接操作备份平台，这些备份平台手动维护恢复。
充分利用厂商的两阶段验证作为安全墙拦截 AI 的敏感操作。
类似于要更高频的把 AI 当成黑客防着。
